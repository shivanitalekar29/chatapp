<div class="row">
  <div class="col-12 col-md-3">
    <div class="mb-3">
      <%= link_to new_room_path, class: "btn btn-outline-success" do %>
        Create a room
      <% end %>
    </div>

    <% if @rooms.present? %>
      <nav class="nav flex-column">
        <% @rooms.each do |room| %>
        <b>  <%= link_to room.name, room_path(room), class: "nav-link room-nav-link bg-success" %> </b>
        <% end %>
      </nav>
    <% else %>
      <div class="text-muted">
        <b>The are no rooms</b>
      </div>
    <% end %>
  </div>

  <div class="col">
    <div class="alert alert-danger">
      <h4 class="alert-heading">
        Welcome to the ChatApp!
      </h4>

      <p>
        We need to talk.
      </p>

      <hr />

      <p>
        You can create or join a room from the sidebar.
      </p>
    </div>
</div>
